[project]
name = "ai-learning-program"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
app = "api.index:app"

[tool.vercel.scripts]
build = "npm --prefix frontend install && npm --prefix frontend run build"
