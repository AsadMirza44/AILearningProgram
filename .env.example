# Copy this file to .env and fill in real values.
# The .env file is gitignored — never commit real keys.

# Anthropic API key — enables AI features in playground modules
# (PromptEditor "Get AI Feedback", Week 4 Code Walkthrough demo)
# Get your key at: https://console.anthropic.com
# If omitted, AI features degrade gracefully — all other activities still work.
ANTHROPIC_API_KEY=sk-ant-...
